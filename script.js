// Function to send email notification
function sendEmailNotification(email, message) {
    // Code to send email using an API or SMTP server
    console.log(`Email sent to ${email}: ${message}`);
}

// Example usage
const registeredEmail = 'example@example.com';
const message = 'New flavor available: Mango Tango!';

sendEmailNotification(registeredEmail, message);

